// -- 9 --
// Créer une fonction qui renvoie le plus petit chiffre d'un array

function smallNumber (arr) {
    let result = arr.indexOf(Math.floor(0));
    console.log(arr.at(result))
}
smallNumber();